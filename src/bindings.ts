export type Bindings = {
  DATABASE_URL: string;
  DATABASE_AUTH_TOKEN: string;
  API_KEY: string;
  RATE_LIMITER: KVNamespace;
};

export type galleryBindings = {
  DATABASE_URL: string;
  DATABASE_AUTH_TOKEN: string;
};